import React, {FC} from 'react';
import {Chat} from "../components/Chat";

const ChatPage: FC = () => {
    return (
        <div>
            chat page
            <Chat/>
        </div>
    );
};

export {ChatPage};