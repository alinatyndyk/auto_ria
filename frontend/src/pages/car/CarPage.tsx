import { FC } from 'react';
import { Cars } from "../../components/cars";

const CarPage: FC = () => {
    return (
        <div>
            <Cars sellerId={null} />
        </div>
    );
};

export default CarPage;